COMPACT_PROMPT = """你是一个对话摘要助手。请将以下对话历史压缩为结构化摘要。

**要求**：
1. 在摘要前，先预览一遍对话历史，确保充分理解用户的意图和对话的上下文
2. 保留所有关键信息：错误后用户的纠正、用户消息、当前工作状态、未完成工作、下一步计划
3. 保留具体的数值、名称、ID 等不可推导的信息
4. 压缩后的摘要不超过300字符
5. 禁止使用任何工具

**输出格式**：
此会话因为上一个会话的上下文耗尽而继续。以下是前面对话的摘要：

用户意图：
- [列出用户的主要意图和变化]
纠正反思：
- [列出用户纠正过的关键点]
当前工作状态：[当前正在做什么]
未完成工作：[还有哪些待完成]
下一步计划：[接下来要做什么]

**例子**：
此会话因为上一个会话的上下文耗尽而继续。以下是前面对话的摘要：

用户意图：
- 开始时用户希望按照JD生成简历。
- 中间用户希望修改简历中的个人优势。
- 最近用户希望修改简历中的项目经历，使经历更匹配JD。
纠正反思：
- 用户希望经历的成果更符合实际情况，而不是过于夸大。
- 用户希望经历的描述更具体，突出实际贡献和技能应用。
当前工作状态：项目经历已更新，等待用户确认。
未完成工作：等待用户确认项目经历是否符合预期。
下一步计划：如果用户确认，继续完善简历；如果用户不满意，根据反馈继续修改项目经历。

{rolling_summary_section}
对话历史：
{history_messages}
"""

ROLLING_SUMMARY_SECTION = """[会话重要点摘要]（辅助参考，不替代对话历史）：
{rolling_summary}

"""


SUMMARY_UPDATE_PROMPT = """你是一个对话摘要助手。请根据当前摘要和最近对话，更新滚动摘要。

**重要**：滚动摘要是记忆系统的上游，需要记录足够多的信息以便后续提炼为持久化记忆。

**要求**：
1. 保留所有关键信息：用户意图变化、已完成的工作、待完成的任务、关键决策
2. 保留具体的数值、名称、ID 等不可推导的信息
3. 识别并记录用户表达的偏好/厌恶/风格倾向（用户偏好）
4. 识别重复出现的工作模式（重复工作流）
5. 记录对后续工作有价值的重要发现
6. 摘要不超过 500 字符
7. 禁止使用任何工具

**输出格式**：
【任务】{场景}
【已完成】（列出已完成的关键步骤和结果）
【待完成】（列出当前目标）
【关键决策】（列出用户做出的不可逆选择）
【用户偏好】（用户表达的喜好/厌恶/风格倾向，如无则留空）
【重复工作流】（多次出现的工作模式，如"反复调整XX（N次）"，如无则留空）
【重要发现】（对后续工作有价值的信息，如无则留空）

当前摘要：
{current_summary}

最近对话：
{recent_messages}
"""


EXTRACT_MEMORY_PROMPT = """你是记忆提炼助手。从以下滚动摘要中识别值得跨会话持久化的信息，这些信息将被写入 Memory.md。

**值得记录**：
- 用户明确表达的偏好/厌恶（如"我喜欢简洁风格"、"不要过度包装"）
- 多次出现的工作模式（如"修改简历时会反复打磨措辞"）
- 用户纠正过的错误（避免再犯，如"成果描述要符合实际"）
- 明确的项目约定/规则（如"简历控制在单页"、"使用STAR格式"）

**不值得记录**：
- 临时任务状态（如"正在修改简历"）
- 容易重新发现的事实（如"Python是一种编程语言"）
- 一次性的上下文信息（如"用户问了XX问题"）
- 过于模糊的信息（如"用户有项目"）

**当前 Memory.md**：
{current_memory}

**滚动摘要**：
{rolling_summary}

**输出规则**：
1. 如果有值得记录的新内容，每条输出一行，格式为：[类别] 内容
   类别必须是以下之一：用户偏好 | 工作习惯 | 项目约定 | 关键教训
2. 如果没有值得记录的新内容，输出：无新内容
3. 不要重复 Memory.md 中已有的内容
4. 每条内容要简洁、信息密度高，不超过 100 字符
"""
